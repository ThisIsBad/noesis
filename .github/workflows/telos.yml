name: telos

on:
  push:
    branches: [master]
    paths:
      - "services/telos/**"
      - "schemas/**"
      - "kairos/**"
      - ".github/workflows/telos.yml"
  pull_request:
    paths:
      - "services/telos/**"
      - "schemas/**"
      - "kairos/**"
      - ".github/workflows/telos.yml"

defaults:
  run:
    working-directory: services/telos

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            schemas/pyproject.toml
            kairos/pyproject.toml
            services/telos/pyproject.toml

      - name: Install schemas
        working-directory: schemas
        run: pip install -e .

      - name: Install kairos
        working-directory: kairos
        run: pip install -e .

      - name: Install telos + dev deps
        run: pip install -e ".[dev]"

      - name: pytest + coverage
        run: |
          python -m pytest \
            --cov=telos.core \
            --cov-report=term-missing \
            --cov-fail-under=85 \
            -v

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            schemas/pyproject.toml
            kairos/pyproject.toml
            services/telos/pyproject.toml

      - name: Install schemas
        working-directory: schemas
        run: pip install -e .

      - name: Install kairos
        working-directory: kairos
        run: pip install -e .

      - name: Install telos
        run: pip install -e .

      - name: Install lint tools
        run: pip install ruff mypy

      - name: ruff
        run: python -m ruff check src/ tests/

      - name: mypy
        run: python -m mypy --strict src/

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    # Build context = repo root; validates Dockerfile + schemas COPY
    defaults:
      run:
        working-directory: .

    steps:
      - uses: actions/checkout@v6

      - name: Build
        run: |
          docker build \
            -f services/telos/Dockerfile \
            -t telos:ci \
            .
