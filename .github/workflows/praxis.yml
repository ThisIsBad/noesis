name: praxis

on:
  push:
    branches: [master]
    paths:
      - "services/praxis/**"
      - "schemas/**"
      - "kairos/**"
      - ".github/workflows/praxis.yml"
  pull_request:
    paths:
      - "services/praxis/**"
      - "schemas/**"
      - "kairos/**"
      - ".github/workflows/praxis.yml"

defaults:
  run:
    working-directory: services/praxis

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            schemas/pyproject.toml
            kairos/pyproject.toml
            services/praxis/pyproject.toml

      - name: Install schemas
        working-directory: schemas
        run: pip install -e .

      - name: Install kairos
        working-directory: kairos
        run: pip install -e .

      - name: Install praxis + dev deps
        run: pip install -e ".[dev]"

      - name: pytest + coverage
        run: |
          python -m pytest \
            --cov=praxis.core \
            --cov-report=term-missing \
            --cov-fail-under=85 \
            -v

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: |
            schemas/pyproject.toml
            kairos/pyproject.toml
            services/praxis/pyproject.toml

      - name: Install schemas
        working-directory: schemas
        run: pip install -e .

      - name: Install kairos
        working-directory: kairos
        run: pip install -e .

      - name: Install praxis
        run: pip install -e .

      - name: Install lint tools
        run: pip install ruff mypy

      - name: ruff
        run: python -m ruff check src/ tests/

      - name: mypy
        run: python -m mypy --strict src/

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    # Build context = repo root; validates Dockerfile + schemas COPY
    defaults:
      run:
        working-directory: .

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: |
          docker build \
            -f services/praxis/Dockerfile \
            -t praxis:ci \
            .
